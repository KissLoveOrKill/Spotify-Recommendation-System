# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/
.Python
env/
venv/
.venv/
.pytest_cache/
*.pyc

# Editor / OS
.vscode/
.DS_Store
Thumbs.db

# Sensitive / local configs
# Ignore real env files but keep `./.env.example` for sharing
.env
.env.local
*.pem
*.key

# Data / model artifacts
# Ignore large raw CSV in the recommender data folder but keep compressed archives
spotify_rec_system/data/dataset.csv
model_cache/
# Allow keeping a placeholder in the recommender module so directory structure is preserved
!spotify_rec_system/model_cache/.gitkeep
*.pth
*.pt
*.npy
*.ckpt
*.h5

# Logs and temporary
logs/
*.log

# Virtual env folders
.venv/
venv/
ENV/

# Git LFS attributes
.gitattributes
