```dotenv
# Example environment variables for Spotify Recommender
# Usage: copy this file to the project root as `.env` and fill the values.
# The app calls `load_dotenv()` in `spotify_rec_system/app.py`, which will
# load `.env` from the current working directory or parent directories.
# Recommended location: project root (same folder as this README).
# Do NOT commit your real `.env` file — it is listed in `.gitignore`.

# Required (create an app at https://developer.spotify.com/dashboard/)
SPOTIPY_CLIENT_ID=your_spotify_client_id_here
SPOTIPY_CLIENT_SECRET=your_spotify_client_secret_here
SPOTIPY_REDIRECT_URI=http://127.0.0.1:5000/callback

# Optional
# A random secret for Flask sessions. If empty, the app may generate one at runtime.
FLASK_SECRET=

```
# Example environment variables for Spotify Recommender
# Copy to `.env` and fill with your credentials (do NOT commit `.env`)

SPOTIPY_CLIENT_ID=
SPOTIPY_CLIENT_SECRET=
SPOTIPY_REDIRECT_URI=http://127.0.0.1:5000/callback
# Optional: Flask secret (you can also rely on random generation)
FLASK_SECRET=
